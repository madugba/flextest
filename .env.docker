# FlexTest Frontend Docker Environment Variables

# API Backend URL (points to backend container in Docker network)
NEXT_PUBLIC_API_URL=http://flextest-backend:3000/v1/api

# Server IPs for admin access
NEXT_PUBLIC_SERVER_IPS=127.0.0.1,::1,localhost
