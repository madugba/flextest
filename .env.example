# FlexTest Frontend Environment Variables

# API Backend URL
# ⚡ AUTOMATIC IP DETECTION ENABLED IN DEVELOPMENT
# Leave commented to auto-detect your local network IP for cross-device access
# The app will automatically replace localhost with your actual IP address
#
# Only set this if you need to override the automatic detection:
# NEXT_PUBLIC_API_URL=http://localhost:3000/v1/api
#
# For production, set your actual API domain:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/v1/api
#
# 💡 Run 'npm run network-info' to see your current network IPs

# Server IPs for admin access (comma-separated)
# Default: 127.0.0.1, ::1, localhost
# Add additional IPs if needed for production
# NEXT_PUBLIC_SERVER_IPS=127.0.0.1,192.168.1.100
